<style>
  .btn-search{
    background-color: #fe424d;
    color:white;
    border-radius:10px;
    
  
  }
  .btn-search:hover{
    background-color: #fe424d;
    color:white;
    border-radius:10px;
    opacity: 0.8;  
  }
  .search-input{
    border-radius:10px;
    
  }
  .acc{
    margin-left:0.7rem;
  }
    #darkModeSwitch:checked {
      background-color: #fe424d;;
      border-color: #fe424d;;
    }
  
    /* Optional: change the toggle knob color */
    #darkModeSwitch:checked::before {
      background-color: white;
    }
  </style>

  <nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fa-solid fa-earth-asia"></i></a>
  
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

        <a class="navbar-brand ms-2 me-4" href="/listings"> <i class="fa-solid fa-house"></i></a>

                 <!-- change -->
       <% if (currUser) { %>
        <a href="/dashboard" class="nav-link"><b>Dashboard </b><i class="fa-solid fa-user acc"></i></a>
      <% } %>
  

        <div class="navbar-nav ms-auto">
          <form class="d-flex me-5" role="search" method="post" action="/search">
            <input class="form-control me-2 search-input" type="search" placeholder="Search Destinations" aria-label="Search" name="title">
            <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass">&nbsp;&nbsp;Search</i></button>
          </form>
  
          <a class="nav-link" href="/listings/new">Become a Host</a>
  
          <% if (!currUser) { %>
            <a class="nav-link" href="/signup"><b>Sign up</b></a>
            <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>
          <% if (currUser) { %>
            <a class="nav-link" href="/logout"><b>Log out</b></a>
          <% } %>

          <div class="form-check form-switch d-flex align-items-center gap-0 ms-0 ms-md-3 ">
            <input class="form-check-input" type="checkbox" id="darkModeSwitch">
            <label class="form-check-label" for="darkModeSwitch">🌙Dark Theme</label>

    
          </div>
          
        </div>
      </div>
    </div>
  </nav>
  <script>
    const toggleSwitch = document.getElementById('darkModeSwitch');
  
    // Set initial theme based on localStorage
    if (localStorage.getItem('theme') === 'dark') {
      document.body.classList.add('dark-mode');
      toggleSwitch.checked = true;
    }
  
    // Toggle dark mode and save to localStorage
    toggleSwitch.addEventListener('change', function () {
      if (this.checked) {
        document.body.classList.add('dark-mode');
        localStorage.setItem('theme', 'dark');
      } else {
        document.body.classList.remove('dark-mode');
        localStorage.setItem('theme', 'light');
      }
    });
  </script>
  